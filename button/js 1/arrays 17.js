let n = [23,4,2,6,5.3,4]

let b = n.toString()// tosring() is convert the an arrays to string

console.log(typeof b) // typefo the help the find the type of like string, function, object;

let c = n.join('and') //join is used to add some things than we use .join()
console.log(c)
console.log(n.pop())// pop()are used to remove the last number of the arrays.
console.log(n)
let r = n.push(45)// puch is the add the elements of the last .opposite of the pop()
console.log(n)


let z = n.shift(34) /// shift() is the remove the first elements of the arrarys and print the new arrays.
console.log(n)
let k = n.unshift(56)
console.log(n)