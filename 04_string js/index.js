// const name = 'dipu';
// const repoNumber = 56;
// // console.log(name + repoNumber+"dipu repocount"); it is a old methot to console to the veriable;

// // console.log(`hii my self ${name} and my totla repo in the github is ${repoNumber}`); it is new method to represent the variable to the user
//  // It is also know as the string inpulations;
//  /// string is a objecti to a key value pair;

//  const car = new String("i learn js programing language"); // when you run in the browser console than behave like a array but it is a string;
//  console.log(car);
//  console.log(car.__proto__);// give me a empty object;
//  console.log(car.__proto__.String) // it give you undefined; but you run is the console give you string;

//  console.log(car.length); /// to acces the without the __proto__ . direct acces the functions;
//  console.log(car.toUpperCase()); // functions; to convert the totla string capital letters;
//  console.log(car.charAt(8)); // it can provid the 8 index position characters;
//  console.log(car.charAt(7)); // white space;
//  console.log(car.indexOf('a'));// 1st a is the give the answerr mean postion and index;
//  console.log(car.substring(0,5)); // pring the 1st o to 4 characters;
// //  console.log(car.slice(-6, 5));


// const number = (4,3,2,64,-9,-88 ,-9);
// console.log(Math.max(number));
// console.log(Math.abs(number));
// console.log(Math.min(number));